import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  leftIcon: {fontSize: 28, marginRight: 5},
  rightIcon: {fontSize: 28, marginLeft: 5},
  iconText: {fontSize: 18},
});

export default styles;
